const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2); // Cálculo da área do círculo

console.log(area); // Imprime a área calculada
console.log(typeof Math); // Imprime o tipo do objeto Math (que é um objeto embutido em JavaScript)
